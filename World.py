class AgentManager(object):
    agents = None #array of agents
    
    def __init__(self):
        self.agents = None
    
    def spawnAgents(self, number, options):
        #options will have probabilities
        #eg variety of topic choice, personality
        
    def addAgent(self, options):
        agent = Agent()
        agent.doSomething()
        
        agents.append(agent)
        
    